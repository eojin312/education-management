<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org/DTD config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="hachi.user">
    <select id="selectAll" resultType="hachi.user.User">
        select name, id, gender, email from member
    </select>
</mapper>